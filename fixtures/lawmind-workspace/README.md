# LawMind CI fixture workspace

Minimal workspace for `pnpm lawmind:gate -- --workspace fixtures/lawmind-workspace --all --strict` in deliverable-gate CI.

Do not store secrets or customer data here.
