{
  "name": "lawmind-docs",
  "private": true,
  "type": "module",
  "description": "LawMind product docs site (VitePress) — lawyer-facing landing + synced docs/LAWMIND-*.md",
  "scripts": {
    "sync": "node ./scripts/sync-docs.mjs",
    "dev": "pnpm run sync && vitepress dev docs",
    "build": "pnpm run sync && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.42"
  }
}
